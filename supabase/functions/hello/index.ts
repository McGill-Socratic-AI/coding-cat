// Placeholder edge function. Deployed during B1 to verify the deploy
// pipeline; replaced/deleted in B3 when the real `analyze` function lands.

Deno.serve(() => new Response("hello from edge function"));
